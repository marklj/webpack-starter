@import "fonts.css";

body {
  font-family: 'Lato', sans-serif;
  background: #EFEFEF;
  color: #222;
  font-size: 1.1em;
}

h1 {
  font-weight: 900;
  font-size: 2.5em;
}

.flag {
  max-width: 400px;
}

.Component {
  color: green;

  &__thing {
    font-size: 40px;

    &--modifier {
      font-weight: bold;

    }
  }
}
